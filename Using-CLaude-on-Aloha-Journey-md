# My journey with using Claude on my Aloha specifictions and machine

## 2026-08-22
- set up git and github remote repo
- asked Claude what it understands from the Aloha-specifictions_v01.md: responds with good concise summary of the contents.
- asked Cluade, if Aloha is Turing-complete: "So structurally, Aloha is a minimal but complete register-machine-style ISA — it's in the same computational class as Minsky machines or a simple RAM model." It comes with some more explanation, why it comes to this conclusion.
- asked Claude, if it can write a Typescrip/node.js virtual machine for Aloha: 
  - found 2 inconsistencies in the specs which I could resolve quickly. It made even resolution specifications, great!
- asked Claude to create the aloha and alcomp programs: it complains about another spec inconsistency around the spec of the import command. I fix it. It produces in about 8 min the code and also has a couple of remarks for me for futire improvements.
  - cannot find the Typescrip code for aloha and alcomp, only the js versions: gives me the syntax how to call: OK
  - I run my own tests: does not work as the Aloha code seems to be hardcoded to 4 input values. It fails on more inputs as it ignores the additionally avilable input arguments. Claude seem to have taken the one runtime example in the Aloha specs and created an "overfitted" test case for it. It did not generalize for any number of input arguments (yet).   
  - alcomp can only read csv with commas as separator: it offers me to auto-detect the separation character for the file and does it after my approval
  - I have alcomp compile my version of findMaxNumber: it throws a compiler error as one define assigns not a number but an expression to the constant. Claude refers to the specification where I the corrected the specs for define.
  - I asked if now the document ist OK: it comes up with some points. One is about jump targets: it seems it did not expect that jump targets are replaced at loading time not at compile time. I modified the specification and let it recheck:
    - it finds jump targets being allowed in goto commands only narrow-minded. It does not realize that other commands do not have jump targets but only target cell where to store a result.
    - As pointed out by Claude, I clarified the difference between the load file into the runtime, i.e. Executable code, and the Machine internal representation. 
    - Claude implies itself that jump targets can be checked at complile-time but resolved not before at load time. Great. I clarify that in the specification for easier understanding. It suggests an explicit clarification in my specifiction. I ask it to do it for me. Result is great.
    - I ask Claude to fix the bug that it does not load the code to the cell I specify in the aloha command. Claude realizes the intend of that feature and reflects on it sensationally!
    - I ask Claude to make the necessary changes in the code. Without asking for, it tests its implementation by running tests with varying aloha command parameters to see if it still works when it locates the code in different places in the Machine memory.
    - Claude gives a good summary of what it changed.  

## 2026-08-23
- I try to compile my examples/findMaxNumberGL01.asm.csv with "alcomp": syntax errors as I am using import with a target cell which conflicts my latest spec where import does not take an argument. I add to the spec a new command "importTo".
- Claude detects a ambiguity in my specs between import and importTo: they could interfere but Claude makes the right assumption in favor of my intention. Anyway, I clarify this in the specification: Claude confirms consistency of my specs now.
- Ask Claude to make the necessary changes to the code.
- The compile of examples/findMaxNumberGL01.asm.csv fails: alcomp cannot find the jump tatget "loop", however, there is no isolated symbol "loop" in the Assembler code. I confront Claude with it: it can explain what is wrong with a regex alcomp uses and suggest to add "-" as a regular character in a symbol. Claude makes the necessary code changes. 
- It point out that there are more difficulities coming from a) my command constant "nil" that alcomp uses to substitute nil values. b) there is no sound solution yet to signal end of importing input values....well observed!  
- I made changes to the specs: nil indicates end of inputs and allowing nil as operand in ifxxGoto commands.
- Claude suggest 2 options how to resolve the "ifGtGoto 5 nil somewhere" semantical ambiguity: I advice to throw a runtim error then Claude makes code changes. I run "node dist/bin/aloha.js examples/findMaxNumberGL01.machine.csv 0 45 100 34 6 98 9
 37 67 301 77 39 111", which results correctly to 301.
 - Claude hat ca. 650 LoC produziert. Code sieht nicht gerade "elegant" aus, aber man kann ihn verstehen. Bin sehr sicher, dass mein Code sehr anders aussehen würde. Ob besser ist tbv. 